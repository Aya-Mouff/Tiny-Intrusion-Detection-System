# Tiny-Intrusion-Detection-System
A LAN-based Intrusion Detection System (IDS) built using classical machine learning algorithms to detect and classify seven specific network attacks (including ARP poisoning, TCP SYN floods, and DNS spoofing).
